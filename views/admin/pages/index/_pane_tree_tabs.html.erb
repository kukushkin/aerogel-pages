<div class="top-toolbar">
    <ul class="nav nav-tabs">
        <% Page.available_langs.each do |tab_lang| %>
            <li <%= @lang.to_s == tab_lang.to_s ? 'class="active"' : '' %> >
                <%= link_to "#{tab_lang}-", tab_lang.to_s,
                    class: "lang-select-link", 'data-lang-id' => tab_lang
                %>
            </li>
        <% end %>
        <li class="pull-right dropdown">
            <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                <%= icon 'fa-cog' %> <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
                <!--
                <li class="disabled">
                    <a href="#">item 1</a>
                </li>
                <li class="divider">
                </li>
                -->
                <li class="pages-reorder-link">
                    <a href="#" class="pages-reorder-link">
                        <span class="checkmark"><%= icon 'fa-check' %></span>
                        <%= t.aerogel.admin.pages.reorder %>
                    </a>
                </li>
            </ul>
        </li>
    </ul>
</div>
