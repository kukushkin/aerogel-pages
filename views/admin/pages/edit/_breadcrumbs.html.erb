<ol class="breadcrumb">
<% @page.parent.ancestors_and_self.sort_by(&:depth).each do |p| %>
    <li>
        <%= link_to "#{@lang}-#{p.id}", p.content(@lang).try(:title) %>
    </li>
<% end if @page.parent.present? %>
    <li>
        <%= @page.page_type.try(:name) %>
    </li>
</ol>