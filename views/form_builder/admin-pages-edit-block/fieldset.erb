<fieldset class="<%= fieldset.object_name %> <%= 'fieldset-with-errors' if fieldset.invalid? %>  id-<%= fieldset.object.id %> page_block">
    <%= content %>
    <%= fieldset.object.persisted? ? fieldset.hidden(:id) : '' %>
</fieldset>
